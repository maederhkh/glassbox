# Environment
.env
.venv/
venv/
.python-version

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Experiment outputs (large, regenerable)
output/
runs/
*.jsonl.gz

# LEXam question and answer text - re-fetched from Hugging Face by
# scripts/select_questions.py. Only ids and metadata are versioned, so the
# repository never redistributes the benchmark's content.
data/cache/

# Checklists derived from LEXam reference answers - same restriction as data/cache/
data/checklists/

# Local caches
.uv-cache/
.uv-python/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
